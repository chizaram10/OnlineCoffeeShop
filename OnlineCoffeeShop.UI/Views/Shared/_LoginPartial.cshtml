@using Microsoft.AspNetCore.Identity

@{
    bool isAuthenticated = (Context.Session.GetString("IsAuthenticated") == "true");
    string username = Context.Session.GetString("Username")!;
}

@if (isAuthenticated)
{
    <form asp-controller="Auth" asp-action="Logout" method="post" id="logoutForm" class="navbar-right">
        <ul class="nav navbar-nav d-flex flex-row navbar-right align-items-center">
            <li>
                Welcome @username!
            </li>
            <li>
                <button type="submit" class="btn btn-link navbar-btn navbar-link fw-bold">Logout</button>
            </li>
        </ul>
    </form>
}
else
{
    <ul class="nav navbar-nav d-flex flex-row navbar-right align-items-center">
        <li><a asp-controller="Auth" asp-action="Register" class="fw-bold">Sign Up</a></li>
        <li><a asp-controller="Auth" asp-action="Login" class="fw-bold">Sign In</a></li>
    </ul>
}
